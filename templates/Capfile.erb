require 'capistrano'
require 'rubygems'
require 'puppet/capistrano'

set :application, "Puppet Master Bootstrap"
set :deploy_to, "<%= puppet_bootstrap_deploy_to %>"
set :deploy_via, :export
set :use_sudo, false
set :user, "vagrant"
set :password, "vagrant"
set :scm, :git
set :repository, "git://github.com/croomes/puppet-conf.git"

role :web, 'localhost'
